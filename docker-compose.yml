version: "3"

services:
  base:
    build:
      context: docker-common
      dockerfile: Dockerfile.jre8
    image: centos-base:jre8
    entrypoint: /bin/bash

  scakafka-proxy:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: scakafka-proxy:0.9.0-SNAPSHOT
    depends_on:
      - base
